spring:
  application:
    name: user-service
  main:
    web-application-type: reactive
server:
  port: 6061
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
